---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class={`section ${className} py-7 text-center flex flex-col items-center justify-center w-full mx-auto container lg:w-[740px] px-3.5 md:px-0 `}
>
    <slot />
</section>
